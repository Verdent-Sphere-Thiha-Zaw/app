name: "CI Workflow"

on:
    push:
      branches:
        - dev
        - main
    pull_request:
      branches:
        - dev
        - main

jobs:
  build:
    name: Build
    uses: ./.github/workflows/build.yaml
    with:
      php-version: "8.2"

  test:
    name: Test
    needs: [build]
    uses: ./.github/workflows/test.yaml
    with:
      php-version: "8.2"

  push:
    name: Push
    needs: [test]
    uses: ./.github/workflows/push.yaml

    # push:
    #   name: Push
    #   needs: [test]
    #   uses: ./.github/workflows/push.yaml
    #   runs-on: ubuntu-latest
    #   steps:
    #     - name: My Step
    #       uses: ./.github/workflows/push.yaml
    #       with:
    #         uname: ${{ secrets.DOCKERHUB_USERNAME }}
    #         pwd: ${{secrets.DOCKERHUB_TOKEN}}



